from setuptools import setup

setup(name='advanced_pca',
      version='0.1',
      description='PCA with varimax rotation and feature selection',
      packages=['advanced_pca'],
      zip_safe=False)
